<UserControl x:Class="RadioV2.Controls.StationListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:helpers="clr-namespace:RadioV2.Helpers"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="600"
             Background="Transparent">

    <Grid Margin="8,4" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Logo -->
        <Border Grid.Column="0"
                Width="40" Height="40"
                CornerRadius="4"
                Background="{ui:ThemeResource ControlFillColorSecondaryBrush}"
                Margin="0,0,12,0"
                VerticalAlignment="Center">
            <Image Stretch="UniformToFill"
                   Width="40" Height="40"
                   Source="{Binding LogoUrl}" />
        </Border>

        <!-- Station name + group -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{Binding Name}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="{Binding Group.Name}"
                       FontSize="12"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Column="2" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden" />
                        <Style.Triggers>
                            <!-- Always show on hover -->
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <!-- Show when favorited, but only when not in hover-only mode -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsFavorite}" Value="True" />
                                    <Condition Binding="{Binding HoverOnlyHeart, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <!-- Plain icon (no button chrome) shown when favourited but not hovering -->
                <ui:SymbolIcon Symbol="Heart24"
                               Filled="True"
                               Margin="11,5"
                               Foreground="{Binding IsFavorite, Converter={StaticResource BoolToHeartColorConverter}}">
                    <ui:SymbolIcon.Style>
                        <Style TargetType="ui:SymbolIcon">
                            <Setter Property="Visibility" Value="Hidden" />
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding IsFavorite}" Value="True" />
                                        <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:SymbolIcon.Style>
                </ui:SymbolIcon>

                <!-- Button with animation, shown only on hover -->
                <ui:Button Appearance="Transparent"
                           Command="{Binding DataContext.ToggleFavouriteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                           CommandParameter="{Binding}"
                           ToolTip="Favourite">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                            <Setter Property="Visibility" Value="Hidden" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                    <ui:Button.Triggers>
                        <EventTrigger RoutedEvent="Button.Click">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="HeartIcon"
                                                                   Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
                                        <EasingDoubleKeyFrame KeyTime="0" Value="1" />
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="1.4">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut" />
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.22" Value="1">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <QuadraticEase EasingMode="EaseIn" />
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                    </DoubleAnimationUsingKeyFrames>
                                    <DoubleAnimationUsingKeyFrames Storyboard.TargetName="HeartIcon"
                                                                   Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
                                        <EasingDoubleKeyFrame KeyTime="0" Value="1" />
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.1" Value="1.4">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut" />
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                        <EasingDoubleKeyFrame KeyTime="0:0:0.22" Value="1">
                                            <EasingDoubleKeyFrame.EasingFunction>
                                                <QuadraticEase EasingMode="EaseIn" />
                                            </EasingDoubleKeyFrame.EasingFunction>
                                        </EasingDoubleKeyFrame>
                                    </DoubleAnimationUsingKeyFrames>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ui:Button.Triggers>
                    <ui:SymbolIcon x:Name="HeartIcon"
                                   Symbol="Heart24"
                                   Filled="True"
                                   RenderTransformOrigin="0.5,0.5"
                                   Foreground="{Binding IsFavorite, Converter={StaticResource BoolToHeartColorConverter}}">
                        <ui:SymbolIcon.RenderTransform>
                            <ScaleTransform />
                        </ui:SymbolIcon.RenderTransform>
                    </ui:SymbolIcon>
                </ui:Button>
            </Grid>

            <!-- Star button: Debug builds only, hover to reveal -->
            <Grid Grid.Column="1"
                  Visibility="{Binding Source={x:Static helpers:DebugHelper.IsDebugBuild},
                               Converter={StaticResource BoolToVisibilityConverter}}">
                <ui:Button Appearance="Transparent"
                           Command="{Binding DataContext.ToggleFeaturedCommand,
                               RelativeSource={RelativeSource AncestorType=Page}}"
                           CommandParameter="{Binding}"
                           ToolTip="{Binding IsFeatured,
                               Converter={StaticResource BoolToFeaturedTooltipConverter}}">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                            <Setter Property="Visibility" Value="Hidden" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                    <ui:SymbolIcon Symbol="{Binding IsFeatured,
                        Converter={StaticResource BoolToStarIconConverter}}" />
                </ui:Button>
            </Grid>

            <ui:Button Grid.Column="2"
                       Appearance="Transparent"
                       Command="{Binding DataContext.PlayStationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                       CommandParameter="{Binding}"
                       ToolTip="Play">
                <ui:SymbolIcon Symbol="Play24" />
            </ui:Button>
        </Grid>
    </Grid>
</UserControl>
