<UserControl x:Class="RadioV2.Controls.StationListItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="600">

    <Grid Margin="8,4" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Logo -->
        <Border Grid.Column="0"
                Width="40" Height="40"
                CornerRadius="4"
                Background="{ui:ThemeResource ControlFillColorSecondaryBrush}"
                Margin="0,0,12,0"
                VerticalAlignment="Center">
            <Image Stretch="UniformToFill"
                   Width="40" Height="40"
                   Source="{Binding LogoUrl}" />
        </Border>

        <!-- Station name + group -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center">
            <TextBlock Text="{Binding Name}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="{Binding Group.Name}"
                       FontSize="12"
                       Opacity="0.7"
                       TextTrimming="CharacterEllipsis"
                       Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Column="2"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <ui:Button Appearance="Transparent"
                       Command="{Binding DataContext.PlayStationCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                       CommandParameter="{Binding}"
                       ToolTip="Play">
                <ui:SymbolIcon Symbol="Play24" />
            </ui:Button>
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Hidden" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsFavorite}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <ui:Button Appearance="Transparent"
                           Command="{Binding DataContext.ToggleFavouriteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                           CommandParameter="{Binding}"
                           ToolTip="Favourite">
                    <ui:SymbolIcon Symbol="Heart24"
                                   Foreground="{Binding IsFavorite, Converter={StaticResource BoolToHeartColorConverter}}" />
                </ui:Button>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
